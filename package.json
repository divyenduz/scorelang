{
  "name": "scorelang",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter=* build",
    "tsc": "bun run --filter=* tsc",
    "lint": "bun run --filter=* lint"
  }
}
